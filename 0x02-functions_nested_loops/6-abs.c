#include "main.h"

/**
*_abs - Compute the absolute value of an integer
*@c: The numbrer to be computed
*Return: for absolute value of 
zero
*/

int _abs(int c)
{
if (c < 0)
{
int abs_val;

abs_val = c * -1;

return (abs_ val);

} 

return (0);
}
